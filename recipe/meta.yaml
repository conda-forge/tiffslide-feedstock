{% set name = "tiffslide" %}
{% set version = "4.0.0" %}
{% set python_min = "3.12" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 8e056c1784b591249f15fed1a68edf69f641602817a5e9763d4aa97c1457e891

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python {{ python_min }}
    - setuptools-scm
    - setuptools
  run:
    - fsspec >=2024.10.0
    - imagecodecs
    - pillow >=9.1.0
    - python >={{ python_min }}
    - tifffile >=2025.5.21
    - typing-extensions >=4.0
    - zarr >=3.0,<4.0

test:
  imports:
    - tiffslide
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/bayer-science-for-a-better-life/tiffslide
  summary: A drop-in replacement for openslide-python.
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - ap--
    - sarthakpati
    - sdvillal
